import 'package:flutter/material.dart';

class WeekEmotion extends StatelessWidget {
  const WeekEmotion({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text(),

        ],
      ),
    );
  }
}
